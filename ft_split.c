








































/*
char    **ft_split(char *str)
{
    char **res;
    int i = 0;
    int j = 0;
    int k = 0;

    res = malloc(10000);
    while((str[i] == ' ' || str[i] =='\t') && str[i++])
    while(str[i])
    {
        res[j] = malloc(10000);
        while ((str[i] != ' ' || str[i] !='\t') && str[i])
        {
            res[j][k] = str[i];
            k++;
            i++;
        }
        while((str[i] == ' ' || str[i] =='\t') && str[i])
            i++;
        k = 0;
        j++;
    }
    return(res[j + 1] = 0);
}*/
